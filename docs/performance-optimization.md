# 阅读与目录性能优化

2026-09-10：根据代码审查逐项处理。保留离线访问、进度持久化、双语阅读、文字选择和语义锚点恢复。

| 编号 | 问题 | 实现 | 状态 | 验证记录 |
| --- | --- | --- | --- | --- |
| 1 | 停滑保存进度时扫描全部缓存、重复淘汰 | 按章节 SQL 更新访问时间；保存进度不再淘汰；未超限时仅 SQL 求和 | 已完成 | 数据库及持久化测试 21 项通过；覆盖时间单调性、章节隔离、LRU 和恢复 |
| 2 | 目录分页全量读取历史摘要、逐条提交 | 按当前页 ID 查询；整页事务写入 | 已完成 | 内容存储、协调器及真实 SQLite 重开回归 38 项通过；覆盖批量回滚及无关损坏行隔离 |
| 3 | 目录更新连带重算书架和重建其他页 | 各 feed 独立通知；无关分页不刷新书架；章节排序使用预计算索引 | 已完成 | 目录控制器、外壳、书架及应用持久化 45 项通过；确认无关目录刷新保持外壳及书架对象；另有发现页后备数据切换保留状态、搜索更新不重建发现页的回归 |
| 4 | 连续阅读无限累积章节与段落索引 | 正文超过 8 章时在滚动空闲且无选区时裁剪至当前章附近；会话 LRU 保留 12 章；清理旧索引 | 已完成 | 阅读及窗口测试 60 项通过；另有逐帧位置不变检查通过，覆盖双向加载及淘汰后重载 |
| 5 | 搜索输入和界面变化重复过滤、复制、排序 | 仅计算当前模式需要的数据；远端顺序直接复用；本地结果按数据和条件缓存 | 已完成 | 外壳及目录测试 35 项通过；验证展开筛选不扫描列表、查询变化会重新计算 |
| 6 | 每次滚动通知扫描锚点，菜单变化重建正文 | 每帧合并采集、单次遍历；ValueNotifier 隔离菜单更新 | 已完成 | 菜单显隐保持正文 Widget 对象；停滑仍保存进度，原有语义锚点恢复回归通过 |
| 7 | 插图尺寸监听未释放 ImageInfo | 在 finally 中释放尺寸监听收到的图像引用 | 已完成 | 使用实际解码测试图；移除正文并清空缓存后仅剩测试自身的 1 个图像句柄 |
| 8 | 插图、文库封面按原图解码 | 插图按阅读宽度与视口较小值、封面按实际宽度乘 DPR 解码；尺寸监听和显示共享 provider | 已完成 | 验证 430px、DPR=1 的视口使用 398px 解码宽度，并保持图片宽高比 |
| 9 | 排行榜一次创建所有结果 Widget | SliverList.separated 按需创建结果；复用远端结果列表 | 已完成 | 200 条结果只构建视口附近的卡片，滚动后仍有上限；筛选和追加分页回归通过 |
| 10 | 横向阅读重复测量并生成整个窗口分页 | 按正文窗口、排版参数、TextScaler、默认文字样式和视口缓存最近一次分页 | 已完成 | 添加书签复用同一测量片段；视口改变重新生成；超长段落分页及位置恢复回归通过 |
| 11 | EPUB 解压与章节 XML/资源处理阻塞 UI isolate | Isolate.run 执行解析和 HTML 生成；丢弃已过期的章节生成结果 | 已完成 | 后台与同步输出一致，异常正确返回；磁盘缓存、损坏文件清理、WKWebView 原有契约回归通过 |

验证以相关行为测试、最终静态分析与完整回归为基础；性能记录区分机制验证与实际帧耗时。设备 Profile 数据另行注明设备、构建模式、场景及测量结果，不以测试通过代替帧率结论。

EPUB 当前使用一次性 isolate，每次章节生成会复制文档到后台。这样不新增常驻 worker 生命周期；若设备测量显示大型文库的传输成本明显，再改为后台保留文档。当前未测量移动设备 Profile 帧耗时，本文不宣称 FPS 或掉帧率提升幅度。

最终完整回归：`cd app && flutter --no-version-check test --no-pub`，311 项通过（包含真实 WKWebView EPUB 契约检查）。

静态分析：Dart MCP 对 `lib`、`test`、`integration_test` 返回无错误；`flutter --no-version-check analyze --no-pub lib test integration_test` 返回 `No issues found`。全目录命令会额外扫描 `build/scroll-diagnosis-20260910` 内已有的外部 Flutter SDK 诊断副本；本次不修改这些生成材料。
