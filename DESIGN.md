---
name: JFZ Reader
description: 现有 Flutter 阅读应用的设计基线
colors:
  app-seed-light: "#397B62"
  app-seed-dark: "#78B99A"
---

# JFZ Reader 设计基线

## Overview

本文件记录 2026-09-14 的代码事实与已有交互约定，供后续优化沿用。
尚未进行本轮截图评审；现有实现不是质量认证，缺陷应基于证据修正。
产品任务与范围见 [PRODUCT.md](PRODUCT.md)。

界面使用 Material 3，以文字、语义颜色和标准控件组织内容。后续优化优先考虑
信息层级、可扫描性、阅读舒适度与操作连贯性。先改善具体页面，再将验证有效的
共用决定更新到此文件。

## Colors

应用主题由 [main.dart](app/lib/main.dart) 的 `ColorScheme.fromSeed` 生成，
浅色与深色的种子值在 frontmatter 中记录。种子色不等于最终的 `primary` 色；
组件使用 `Theme.of(context).colorScheme` 的语义角色。

阅读器外观由 [ReaderPaletteColors](app/lib/features/reader/reader_controls.dart)
单独解析，支持自动、纸张、暖纸、低光、深色、纯黑。保持这些阅读偏好与应用主题
分离。纯黑是已有阅读选项，通用的“避免纯黑”规则不适用于取消此功能。

日文辅助文字对比度要在具体背景、透明度及字号下验证，不能仅凭颜色名称判断。

## Typography

应用使用系统字体，回退序列为 PingFang SC、Noto Sans CJK SC、Hiragino Sans。
界面字号优先沿用 `TextTheme`；中日文字形、长标题及系统文字缩放是评审重点。
不因通用网页审美建议引入装饰字体或大型 CJK 字体文件。

Web Novel 的当前默认值来自
[ReaderSettings](app/lib/core/model/reader_models.dart)：

| 项目 | 当前默认值 |
| --- | --- |
| 中文 / 日文正文 | 22 / 16 Flutter 逻辑单位，仍需结合文字缩放 |
| 行高 / 段间距 | 1.8 / 15 |
| 日文透明度 | 0.56，有译文时作为次要文本 |
| 字体 / 字重 | 系统无衬线 / 常规；可选系统衬线与加粗 |

这些是可调整的用户默认值，不是所有屏幕的固定字号。文库 EPUB 使用独立排版链路，
不把这组参数直接套到其正文；日文原文回退也不能继续被弱化成不可读的次要信息。

## Layout

- 应用一级导航为“发现、搜索、书架、设置”。
  [NoveliaShell](app/lib/features/shell/novelia_shell.dart) 在宽度小于 840 时使用
  底部导航，达到 840 时使用侧边导航栏，并保留各页状态。
- Web Novel 默认页边距为 24，最大阅读宽度为 720。
  [双栏判断](app/lib/features/reader/reader_pagination.dart) 在视口宽度达到 1000
  且用户未选单栏时允许中日并列。该断点是当前行为，需结合实际窗口验证。
- 小说列表复用卡片，卡片内边距为 16。长中文标题、日文副标题、元信息、译文覆盖、
  标签和进度竞争空间，是首轮评审的重点，不能仅通过缩小文字解决。
- 导航断点、阅读宽度及双栏断点用途不同；调整时分别验证。触控区域、安全区域、
  键盘遮挡、横屏和折叠姿态按目标平台检查。

## Elevation & Depth

当前使用 Flutter Material 组件提供的表面、卡片和弹层层级，尚无独立的全局阴影
token 系统。优化优先沿用主题和组件语义，不为统一文档而杜撰阴影参数。

## Shapes

全局沿用 Material 3 组件形状；局部已有圆角，例如小说标签为 8、来源标识为 7。
这些是 [CatalogNovelCard](app/lib/features/discover/catalog_card.dart) 中的局部值，
不是已经建立的全局圆角尺度。

## Components

| 组件或界面 | 当前约定与优化边界 |
| --- | --- |
| 小说卡片 | 中文标题优先；日文标题、作者、状态、来源、译文覆盖和标签辅助判断。卡片打开作品，标签是独立操作。保留清晰的语义标签与点击区域。 |
| 应用导航 | 四个一级目的地；排行为发现中的次级入口。导航代表目的地，不用于执行下载或设置操作。 |
| 书架 | 续读／离线／收藏为平级分类，支持点击标签和左右滑动同步切换；书签及账号阅读历史固定在顶部。续读与离线使用按需构建的紧凑列表，各自保留搜索、排序和滚动位置；同次运行中切换一级导航、横竖屏或侧边导航均保留状态。最近一条续读适度突出，详情为行尾次级操作。 |
| 离线书籍 | 按小说与译源分别显示，提供完成、未完成、失败、暂停筛选。主操作进入这一译源的本地可读章节；保存位置不可用时，从已下载章节开始。无已存章节时进入下载管理。管理仍保留暂停、继续、重试及删除确认。 |
| 收藏夹书单 | 搜索中／日标题或作者，向服务器查询整个收藏夹。来源多选、类型、分级和翻译放在可滚动的筛选面板，应用后统一查询，取消不改变条件；默认全部来源及分级。更新时间／收藏时间排序直接可切换。改变查询回到第一页，分页与重试保留条件；查看作品返回后刷新当前页并保留滚动位置。 |
| 阅读控制层 | 中央区域切换控制层；边缘点按与翻页手势按用户设置执行。文本选择、翻页和系统返回不应互相抢占。 |
| 阅读外观面板 | 保留即时预览、取消恢复、完成保存及重置约定。排版重排保留语义位置；详见阅读器需求与稳定性记录。 |
| 状态与反馈 | 区分首次加载、追加加载、空结果、离线缓存、失败重试和译文待生成。实际缺陷待逐页核对。 |
| 文库阅读器 | 保留 EPUB 插图、ruby 和卷内导航；在系统 WebView 宿主中验证，浏览器近似效果不能证明设备正确性。 |

## Do's and Don'ts

- 沿用绿色主题、中文优先层级、系统字体与既有阅读设置；明确要求重设计时再决定
  替代方案。
- 针对真实任务评价改动：是否更容易续读、选择作品、核对原文、找到设置。
- 保留缓存、下载、收藏与书签的语义区别；状态变化给出明确反馈。
- 不为装饰增加持续动画、打断阅读的品牌展示或层层嵌套容器。
- 不因套用网页规则而撤掉纯黑阅读选项、系统字体或原生控件行为。
- 不把源码推测或 macOS 截图表述为移动端、HarmonyOS 真机验证结果。

平台参考按任务查阅：[Flutter 自适应布局](https://docs.flutter.dev/ui/adaptive-responsive)、
[Flutter 无障碍](https://docs.flutter.dev/ui/accessibility)、
[HarmonyOS 设计指南](https://developer.huawei.com/consumer/cn/design/)、
[Apple HIG](https://developer.apple.com/design/human-interface-guidelines/)。
